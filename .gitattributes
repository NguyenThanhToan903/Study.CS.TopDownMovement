# Normalize text files to use LF line endings in the repository
* text=auto

# Track binary file types with Git LFS
*.psd filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text
*.ogg filter=lfs diff=lfs merge=lfs -text
*.fbx filter=lfs diff=lfs merge=lfs -text
*.tga filter=lfs diff=lfs merge=lfs -text
*.exr filter=lfs diff=lfs merge=lfs -text

# Unity asset files
*.anim binary
*.controller binary
*.prefab binary
*.unity binary
*.mat binary

# Ignore meta files generated by Unity, but normalize line endings
*.meta text

# Ignore Visual Studio files (for C# projects)
*.csproj text eol=lf
*.sln text eol=lf

# macOS-specific files
*.DS_Store export-ignore
._* export-ignore

# Windows-specific files
Thumbs.db export-ignore
Desktop.ini export-ignore

# Prevent tracking of the Library folder (just for safety, make sure it's also in your .gitignore)
Library/* binary
